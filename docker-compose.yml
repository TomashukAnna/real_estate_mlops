version: "3.9"

services:
  api:
    image: ${API_IMAGE:-real-estate-api:local}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: real-estate-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
