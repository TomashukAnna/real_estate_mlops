version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: real-estate-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
