apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: real-estate-mlops
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/TomashukAnna/real_estate_mlops.git
    targetRevision: main
    path: k8s/overlays/docker-desktop
  destination:
    server: https://kubernetes.default.svc
    namespace: real-estate-mlops
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
